apiVersion: v2
name: learn
description: DClaw Learn — Adaptive learning platform (Next.js + FastAPI + PostgreSQL)
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: cloudnative-pg
    version: "0.21.0"
    repository: "https://cloudnative-pg.github.io/charts"
    condition: database.enabled
    alias: cnpg
